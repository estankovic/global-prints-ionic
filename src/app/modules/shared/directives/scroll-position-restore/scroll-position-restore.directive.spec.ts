import { ScrollPositionRestoreDirective } from './scroll-position-restore.directive';

describe('ScrollPositionRestoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollPositionRestoreDirective();
    expect(directive).toBeTruthy();
  });
});
